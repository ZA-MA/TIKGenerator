<Window x:Class="TIKGenerator.Views.Windows.ImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TIKGenerator.Views.Windows"
        mc:Ignorable="d"
        Title="ImportWindow" Height="250" Width="400" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Выберите файл для импорта:" Margin="0,0,0,5"/>
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="PathBox" Width="250" IsReadOnly="True" Style="{StaticResource TextBoxStyle}"/>
                <Button Content="Обзор..." Margin="5,0,0,0" Width="80" Click="Browse_Click" Style="{StaticResource ButtonStyle}"/>
            </StackPanel>

            <Button Content="Импортировать" Margin="0,20,0,0" Click="Import_Click" Style="{StaticResource ButtonStyle}"/>
            <Button Content="Отмена" Margin="0,10,0,0" Click="CancelButton_Click" Style="{StaticResource ButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
